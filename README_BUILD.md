# V0.7 Build Notes

For the easiest APK build, use GitHub Actions. The workflow regenerates the Android platform files on the runner, so the uploaded repository does not depend on a Gradle wrapper generated on the phone.

Open GitHub → Actions → **Build WorkoutApp APK** → **Run workflow**.

Download the artifact:

`WorkoutApp-v0.7-release`
